# MinIO credentials (dev only)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# MinIO ports published on host
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# Public bucket for images
MINIO_BUCKET=images

# NGINX published port
NGINX_PORT=8081
